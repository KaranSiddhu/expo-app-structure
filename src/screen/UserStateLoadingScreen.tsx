import React from 'react';
import CustomLoader from '../components/common/CustomLoader';

const UserStateLoadingScreen = () => {
	// TODO: when user logged in successfully use this component to get all the user related data
	return <CustomLoader />;
};

export default UserStateLoadingScreen;
